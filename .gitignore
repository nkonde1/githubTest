# ---------------------------------------------------
# Global ignores for the entire embedded-finance-platform
# ---------------------------------------------------

# Environment variables
# These files contain sensitive information or local configurations
# and should not be committed to version control.
.env
.env.*local
!.env.example # Keep .env.example files to show required variables

# Local development and build artifacts
*.log
*.sqlite3
*.db
.DS_Store
Thumbs.db
.ipynb_checkpoints/
.pytest_cache/
__pycache__/

# Docker-related ignores
# Ignore Docker build contexts and volumes that are recreated
# or hold sensitive/temporary data.
.dockerignore # Although often small, it's good practice.
!docker-compose.yml # Ensure main compose file is tracked.

# ---------------------------------------------------
# Backend specific ignores (Python, FastAPI, Celery)
# Located in the 'backend/' directory
# ---------------------------------------------------
/backend/__pycache__/
/backend/*.pyc
/backend/*.egg-info/
/backend/build/
/backend/dist/
/backend/.venv/
/backend/venv/
/backend/alembic/versions/*.pyc # Compiled Alembic migration files
/backend/celerybeat-schedule # Celery beat schedule file
/backend/.pytest_cache/
/backend/.mypy_cache/
/backend/.ruff_cache/
/backend/instance/ # Flask-style instance folder for local config/db if used
/backend/celery*.pid # Celery worker PID files
/backend/celery*.log # Celery worker log files

# Machine Learning related ignores (if models are stored locally and not version controlled)
/backend/ml/models/
/backend/ml/data/
/backend/ml/*.pt # PyTorch models
/backend/ml/*.h5 # Keras/TensorFlow models
/backend/ml/*.joblib # Scikit-learn models
/backend/ml/*.pkl # Python pickle files for models/data

# ---------------------------------------------------
# Frontend specific ignores (React, Vite, Node.js)
# Located in the 'frontend/' directory
# ---------------------------------------------------
/frontend/node_modules/
/frontend/dist/           # Vite build output
/frontend/.vite/          # Vite cache directory
/frontend/.idea/          # IntelliJ/WebStorm IDE files
/frontend/.vscode/        # VS Code settings (if not shared)
/frontend/npm-debug.log*
/frontend/yarn-debug.log*
/frontend/yarn-error.log*
/frontend/pnpm-debug.log*
/frontend/coverage/       # Test coverage reports

# ---------------------------------------------------
# AI Agent specific ignores (Ollama related)
# Located in the 'ai-agent/' directory
# ---------------------------------------------------
# Ollama stores models in ~/.ollama by default, which is mapped via volume
# so the models themselves are not directly in the git repo.
# Any local cache or temporary files generated by ollama_client.py or handlers.py
/ai-agent/__pycache__/
/ai-agent/*.pyc
/ai-agent/venv/
/ai-agent/.venv/